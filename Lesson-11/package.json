{
  "name": "coverage",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "coverage": "nyc --reporter=text npm run test"
  },
  "nyc": {
    "include": [
      "src/**/*.js"
    ],
    "exclude": [
      "test/**/*.js"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "coverage": "^0.4.1",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0"
  }
}
